version: '3'


services:
  project-t:
    container_name: project-t
    build: .
    ports:
      - 1000:8080
    restart: always
    networks:
      - nginx-network

networks:
  nginx-network:
    external: true